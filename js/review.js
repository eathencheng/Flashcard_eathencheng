const r=id=>document.getElementById(id);let reviewWords=[];function renderReview(list){const c=r('reviewList');c.innerHTML='';if(!list.length){c.innerHTML='<div class="empty">目前沒有錯題。可在字卡頁按「↑ 加入錯題本」，或在單字測驗答錯時自動加入。</div>';return}list.forEach(word=>{const card=document.createElement('article');card.className='word-card';card.innerHTML=`<p class="pill">${word.category}｜${word.level}</p><h3>${word.word}</h3><p><strong>中文：</strong>${word.meaning}</p><p><strong>詞性：</strong>${word.partOfSpeech}</p><p><strong>例句：</strong>${word.example}</p><p><strong>翻譯：</strong>${word.translation}</p><p><strong>同義字：</strong>${word.synonyms}</p><p><strong>搭配詞：</strong>${word.collocation}</p><button class="btn small outline remove-btn" data-word="${word.word}">移除</button>`;c.appendChild(card)});document.querySelectorAll('.remove-btn').forEach(btn=>btn.addEventListener('click',()=>{reviewWords=reviewWords.filter(w=>w.word!==btn.dataset.word);setReviewWords(reviewWords);renderReview(reviewWords)}))}function initReview(){reviewWords=getReviewWords();renderReview(reviewWords);r('reviewSearch').addEventListener('input',e=>{const k=e.target.value.toLowerCase();renderReview(reviewWords.filter(w=>w.word.toLowerCase().includes(k)||w.meaning.toLowerCase().includes(k)||w.category.toLowerCase().includes(k)))});r('clearReviewBtn').addEventListener('click',()=>{if(confirm('確定要清空錯題本嗎？')){reviewWords=[];setReviewWords([]);renderReview(reviewWords)}})}document.addEventListener('DOMContentLoaded',initReview);